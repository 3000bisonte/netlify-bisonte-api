{
  "version": 2,
  "framework": "nextjs",
  "devCommand": "next dev ./frontend -p 3001",
  "installCommand": "npm ci --prefer-offline || npm install && cd frontend && (npm ci --prefer-offline || npm install)",
  "buildCommand": "npm run vercel-build"
}
